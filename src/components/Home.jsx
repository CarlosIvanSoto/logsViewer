import React from 'react'
import { View } from 'react-native'
import AppBar from "./AppBar"

const Home = () => {   
    return (
        <View style={{flex:1}}>
            <AppBar title='home' />
        </View>
    )
}

export default Home